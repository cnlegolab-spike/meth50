# 50일 수학 · 상권과 하권 학습실

이 폴더는 기존 학습실과 별도로 만든 새 프로그램입니다. `index.html`을 더블클릭하면 열립니다. 메인 화면에서 로그인 없이 50·60·70·80일 수업계획을 비교할 수 있습니다.

## 콘텐츠와 계획

- 상권 46강(32-1강 포함), 하권 34강: 총 80강입니다.
- 강의 제목, 원래 강의 번호, 탑재일, 강의시간을 반영했습니다. 교재 페이지와 실제 강의 URL은 제공되지 않아 임의로 입력하지 않았습니다.
- 상권 → 하권 순서로 빠짐없이 하루 1~2강을 배정합니다. 50일은 2강인 날 30일, 60일은 20일, 70일은 10일, 80일은 매일 1강입니다.
- 학생 로그인 후 ‘수업계획’과 ‘학습 시작일’을 선택하고 ‘계획 · 일정 적용’을 누릅니다. 주말 포함 연속 일정입니다.
- 계획별 체크·오답·메모는 따로 저장됩니다. 변경한 계획은 처음부터 진행하며, 이전 계획으로 돌아오면 기존 기록이 다시 표시됩니다. 시작일은 공통입니다.
- 진도율·랭킹·관리자 화면·엑셀은 학생의 현재 선택 계획 기준입니다.

## 유지한 기능

학생/관리자 로그인, 계정 발급·삭제, 비밀번호 변경·재설정, 네 가지 학습 활동 체크, 학습 날짜·문항 수·시간·메모 저장, 문항별 오답 이유 기록, 상하권 진도, 시작일 변경, 밀린 학습 알림, 익명 랭킹, 관리자 상세 조회, 강의 링크 관리, 엑셀 내보내기입니다.

## 온라인 사용 설정

원래 학습실의 데이터와 섞이지 않도록 **새 Supabase 프로젝트**를 연결하도록 구성했습니다. 기존 프로젝트에서 아래 초기화 SQL을 실행하지 마세요. 실제 로그인·저장은 설정 후 사용할 수 있습니다.

1. 새 Supabase 프로젝트의 SQL Editor에서 `supabase/install-new-project.sql` 전체를 한 번 실행합니다.
2. Auth에서 직접 회원가입을 끄고 관리자 이메일 `admin@students.center.invalid`의 계정을 만듭니다. 이메일 확인을 완료하고 생성된 UUID로 아래 SQL을 실행합니다.

```sql
insert into public.profiles(id,name,grade,role)
values ('관리자 UUID','센터 관리자','','admin');
```

3. `dist/config.js`에 새 프로젝트 URL과 publishable 또는 anon 공개 키를 입력합니다. 서버 비밀 키는 넣지 않습니다.
4. 이 폴더에서 `node build-single.mjs`를 실행하면 연결 설정을 포함한 실행 파일이 갱신됩니다.
5. Supabase CLI로 새 프로젝트를 연결하고 계정 관리 함수를 배포합니다. `ALLOWED_ORIGINS`는 실제 사이트 출처로 바꿉니다.

```sh
supabase login
supabase link --project-ref 새프로젝트ID
supabase secrets set ALLOWED_ORIGINS=https://센터계정.github.io,http://127.0.0.1:4173
supabase functions deploy manage-student --no-verify-jwt
```

6. 이 폴더의 내용을 새 GitHub 저장소에 업로드하고 Pages의 Source를 GitHub Actions로 설정합니다. 포함된 워크플로는 `dist`를 게시합니다.
7. 관리자 로그인 후 학생 계정을 발급하고 80강의 실제 HTTPS 강의 주소를 등록합니다. 강의 재생·다운로드·자막 자체는 원래 강의 제공 사이트에서 이용합니다.

## 검증

`node check-course.mjs`로 강의 80개, 각 계획의 누락·중복 없는 배정, 계획별 기록 분리, 완료율·완료 강의 수, 학생·관리자 화면 생성을 확인했습니다. `node --check dist/app.js` 구문 검사도 통과했습니다.

실제 Supabase 로그인·서버 저장·권한 정책·계정 발급·랭킹 통합 테스트는 새 프로젝트 연결 후 확인해야 합니다. 서로 다른 학생 두 명으로 기록 분리와 계획 변경 후 복원, 80일차 저장, 엑셀 다운로드를 확인하세요.

각 계획의 5개 구간 마지막 날은 강의 학습과 함께 오답 복습을 진행합니다. 마지막 날은 상하권 전체 점검을 안내합니다.
